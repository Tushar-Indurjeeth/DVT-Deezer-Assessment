/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  images: {
    domains: ['logodownload.org', 'e-cdns-images.dzcdn.net'],
  },
};

module.exports = nextConfig;
